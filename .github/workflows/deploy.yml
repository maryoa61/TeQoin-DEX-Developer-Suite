jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm install
      - run: npx hardhat run scripts/deploy-token.cjs --network teqoin
        env:
          PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
